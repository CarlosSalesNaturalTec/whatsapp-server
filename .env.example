# =============================================================
# Arquivo: .env.example
# Descrição: Exemplo de variáveis de ambiente da aplicação.
#            Copie este arquivo para .env e preencha os valores reais.
#            NUNCA commite o arquivo .env no repositório Git.
# Feature: feat-004 - Configurar variáveis de ambiente
# Criado em: 2026-02-25
# =============================================================

# -------------------------------------------------------------
# AMBIENTE
# -------------------------------------------------------------
# Define o modo de execução da aplicação.
# Valores aceitos: development | production
# - development: logs verbosos (debug), erros detalhados
# - production:  logs reduzidos (warn), erros sanitizados
NODE_ENV=development

# -------------------------------------------------------------
# GCP — Google Cloud Platform
# -------------------------------------------------------------
# ID do projeto GCP onde o Secret Manager está configurado.
# Encontre em: console.cloud.google.com → selecione o projeto → ID no topo
# Exemplo: meu-projeto-123456
GCP_PROJECT_ID=seu-projeto-gcp-aqui

# Nome do secret no Google Secret Manager que armazena a sessão WhatsApp.
# O secret deve ser criado previamente no GCP com replication automática.
# Comando: gcloud secrets create whatsapp-baileys-session --replication-policy="automatic"
SECRET_NAME=whatsapp-baileys-session

# -------------------------------------------------------------
# WHATSAPP
# -------------------------------------------------------------
# Número de telefone da conta WhatsApp que o bot irá gerenciar.
# Formato: E.164 sem o sinal de '+' (apenas dígitos).
# Exemplos: 5511999999999 (Brasil), 15551234567 (EUA)
PHONE_NUMBER=5511999999999

# -------------------------------------------------------------
# SERVIDOR HTTP
# -------------------------------------------------------------
# Porta em que o servidor Express irá escutar requisições HTTP.
# Em desenvolvimento: 3000
# Em produção na VM: pode ser 80 (requer sudo) ou manter 3000 com proxy reverso
PORT=3000

# -------------------------------------------------------------
# LOGGING
# -------------------------------------------------------------
# Nível mínimo de log exibido pela aplicação (biblioteca: pino).
# Valores aceitos (do mais ao menos verboso): trace | debug | info | warn | error | fatal
# - development: use 'debug' para ver todos os eventos do Baileys
# - production:  use 'warn' para reduzir verbosidade e custo de I/O
LOG_LEVEL=warn
